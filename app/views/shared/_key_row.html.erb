<tr class="table-hover">
  <td><%= credential.id %></td>
  <td><%= credential.nickname %></td>
  <td><%= credential.created_at %></td>
  <td><%= button_to 'Delete', dashboard_path(credential), class: 'btn btn-danger btn-sm' %></td>
</tr>
